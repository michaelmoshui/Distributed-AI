import numpy as np
# from mpi4py import MPI

# comm = MPI.COMM_WORLD
# rank = comm.Get_rank()

# output_file = f"output_rank_{rank}.txt"

# with open(output_file, "w") as f:
#     if rank == 0:
#         f.write("Hello from rank 0 (device 1)\n")
#     elif rank == 1:
#         f.write("Hello from rank 1 (device 2)\n")